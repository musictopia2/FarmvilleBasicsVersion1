<h1 class="mb-3">Farm</h1>
<GridTwoColumnUnlimited ColumnGap="5px">
    @foreach (var habitat in state.Habitats)
    {
        <div class="card h-100 text-center" style="width: 40vw;">
            <div class="card-body d-flex flex-column justify-content-between p-2">
                <div class="small">
                    @if (habitat.State == EnumHabitatState.Empty)
                    {
                        <span class="text-muted">Empty</span>
                    }
                    else if (habitat.State == EnumHabitatState.Growing)
                    {
                        <div class="text-primary">
                            Growing 2 Honey
                        </div>
                        <div class="text-muted">
                            @ReadyText(habitat)
                        </div>

                    }
                    else
                    {
                        <span class="text-success">Ready</span>
                    }
                </div>

                <div>
                    @if (habitat.State == EnumHabitatState.Empty)
                    {
                        <PrimaryButton IsEnabled="CanPlant(habitat)" OnClick="() => Plant(habitat)">
                            Plant Honey
                        </PrimaryButton>

                    }
                    else if (habitat.State == EnumHabitatState.Ready)
                    {
                        <SuccessButton OnClick="() => Harvest(habitat)">
                            Harvest
                        </SuccessButton>
                    }
                    else
                    {
                        <button class="btn btn-sm btn-secondary w-100" disabled>
                            Growing…
                        </button>
                    }
                </div>

            </div>
        </div>
    }
</GridTwoColumnUnlimited>
@if (state.Honey > 0)
{
    <div>
        Honey: @state.Honey
    </div>
}
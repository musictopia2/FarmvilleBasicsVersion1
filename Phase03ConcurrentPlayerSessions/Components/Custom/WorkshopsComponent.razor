@inherits FarmComponentBase
<WrapLayout RenderList="_workshops" ColumnWidth="10vw" Context="workshop">
    <PrimaryButton OnClick="() => SelectWorkshop(workshop)">
        @workshop.Name
    </PrimaryButton>
</WrapLayout>
@if (_farms.Count == 0)
{
    return;
}

@foreach (var farm in _farms)
{
    <PrimaryButton OnClick="() => NavigateTo(farm.FarmTheme, farm.PlayerName, farm.SessionMode)">
        @farm.FarmTheme @farm.PlayerName @farm.SessionMode
    </PrimaryButton>
}